import styled from "styled-components"

export default function NotFoundPage(){
    return (
       <TitleWrapper>Página não encontrada</TitleWrapper> 
    )
}

const TitleWrapper = styled.h1`
font-size: 2rem;
color: white;
text-align: center;
margin-top: 1rem;
`
