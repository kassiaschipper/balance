# Install front dependencies
cd balance-app-front && npm install &&
cd ..
echo 'Back Dependencies installed'
# Install back dependencies
cd balance-app-back && npm install && 
cd .. 
echo 'Back Dependencies installed'